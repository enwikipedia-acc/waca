acc.php:
1. More of the interface should be configurable
2. Requests with multiple tries in history should be stripped of links (forcing the user to zoom and examine the circumstances
3. Mail the interface user when thier account has changed status (i.e. promoted, suspended, etc), and whom changed the status
4. Add e-mail confirmation to new ACC users and more importantly account requests.

accounts2.php:
1. Some sort of stats mechanism. Maybe just telling the user where they rank upon !count

users.php:
1. Declines


